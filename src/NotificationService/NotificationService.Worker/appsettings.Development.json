{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.Hosting.Lifetime": "Information"
    }
  },
  "AzureServiceBus": {
    // Use user secrets for development outside Docker. Keep empty if using user secrets or docker-compose env vars
    "ConnectionString": "",
    "TopicName": "ordersevents",
    "SubscriptionName": "notificationsubscription-dev"
  },
  "ApplicationInsights": {
    // Use user secrets for development outside Docker. Keep empty if using user secrets or docker-compose env vars
    "ConnectionString": "" //
  }
}
